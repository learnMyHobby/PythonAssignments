# Ask to enter two numbers (say, a and b). Generate two random numbers
#    between those two numbers and find a combination of these two newly generated
#    random numbers.

import random

# a = int(input('Enter the first number for combination: '))
# b = int(input('Enter the second number for combination: '))

randomNumber = example.randint(3, 5)

print(randomNumber)


